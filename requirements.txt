# Operations dashboard (Flask app + Go API stack) — this is what Render deploys.
# Keep this LEAN: only what platform/server.py needs at runtime.
# RAG / ML pipeline deps live in intelligence/requirements.txt — do NOT add them here.
flask==3.1.1
pandas==2.2.3
requests==2.32.3
gunicorn==23.0.0
psycopg2-binary==2.9.9
mistune==3.0.2

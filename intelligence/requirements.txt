# Intelligence pipeline — ETL, predictions, explanations, and RAG preprocessing.
# These run OFFLINE (locally or in CI), never on the dashboard web service.
# Install: pip install -r intelligence/requirements.txt
# Do NOT add Flask or gunicorn here — those belong in the root requirements.txt.

# Data / ML
pandas==2.2.3
scikit-learn==1.5.2
numpy==2.1.3

# Database + env (etl_to_database.py, import_explanations.py, generate_explanations.py)
psycopg2-binary==2.9.9
python-dotenv==1.1.0
requests==2.32.3

# RAG preprocessing (rag_preprocessing.py, validate_rag_preprocessing.py)
chromadb==1.5.9
sentence-transformers==5.6.0
pdfplumber==0.11.10
tiktoken==0.13.0
nltk==3.9.4
